# ========================================
# ORION BACKEND - ENVIRONMENT CONFIGURATION
# ========================================
# ⚠️  IMPORTANT: Copy this file to .env and configure the values below!
#    cp env.example .env
#    nano .env

# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_USER=orion_user
DB_PASSWORD=your_secure_password_here
DB_NAME=orion

# Google AI API Configuration (Optional - AI features won't work without it)
GOOGLE_API_KEY=your_google_api_key_here

# Application Configuration
ENVIRONMENT=development
DEBUG=true

# ========================================
# Quick Setup Instructions:
# 1. Copy this file: cp env.example .env
# 2. Edit the .env file: nano .env
# 3. Change at least DB_PASSWORD to a secure password
# 4. Optionally add your GOOGLE_API_KEY for AI features
# 5. Run: docker-compose up --build
# ======================================== 