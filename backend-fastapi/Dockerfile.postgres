FROM postgres:15

# Create directory for initialization scripts
RUN mkdir -p /docker-entrypoint-initdb.d

# Copy the initialization SQL script
COPY init.sql /docker-entrypoint-initdb.d/01-init-db.sql

# Expose PostgreSQL port
EXPOSE 5432

# Start PostgreSQL
CMD ["postgres"] 